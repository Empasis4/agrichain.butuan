# Let nixpacks auto-detect PHP and composer from composer.json
# Only override the phases we need to customize

[start]
cmd = "php artisan migrate --force; php artisan serve --host=0.0.0.0 --port=$PORT"
